export const LocalFocusTip = () => {
    return (
        <Tip>
            <strong>此集成专注于通过 STDIO 传输运行本地 FastMCP 服务端文件。</strong> 对于使用 HTTP 或 SSE 传输运行的远程服务端，请使用客户端的原生配置；FastMCP 的集成主要用于简化带依赖和 <code>uv</code> 命令的复杂本地设置。
        </Tip>
    );
};
