export const VersionBadge = ({ version }) => {
    return (
        <Badge stroke size="lg" icon='gift' iconType='regular' className="version-badge">
            版本 <code>{version}</code> 新增
        </Badge>
    );
};
